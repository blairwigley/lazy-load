jQuery(function($) {
	window.addEvent("domready", function() {
		//var loadgif = "//cdnjs.cloudflare.com/ajax/libs/galleriffic/2.0.1/css/loader.gif";
		//$("img").each(function() {
		//	var $img = $(this);
	    //	$img.attr("data-src", $img.attr("src"));
	    //	$img.removeAttr("src");
	    //	$img.attr("src", loadgif);
		//});
		
		var lazyloader = new LazyLoad({
		});
	});

});

